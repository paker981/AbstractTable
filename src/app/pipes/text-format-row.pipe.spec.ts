import { TextFormatRowPipe } from './text-format-row.pipe';

describe('TextFormatRowPipe', () => {
  it('create an instance', () => {
    const pipe = new TextFormatRowPipe();
    expect(pipe).toBeTruthy();
  });
});
