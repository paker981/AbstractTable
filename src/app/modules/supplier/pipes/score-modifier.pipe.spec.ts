import { ScoreModifierPipe } from './score-modifier.pipe';

describe('ScoreModifierPipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreModifierPipe();
    expect(pipe).toBeTruthy();
  });
});
